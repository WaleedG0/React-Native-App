import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 10
  },

  contentContainer: {
    paddingTop: 30
  }
});

export default styles;
